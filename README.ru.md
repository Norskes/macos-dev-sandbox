# macOS Sandbox - Утилита для безопасной разработки

Утилита для изоляции потенциально опасного кода при разработке на macOS, используя встроенный механизм `sandbox-exec`.

## Установка

1. Клонируйте репозиторий:

   ```bash
   git clone https://github.com/your-username/macos-sandbox.git
   cd macos-sandbox
   ```

2. Запустите скрипт установки:

   ```bash
   ./sandbox-setup.sh
   ```

3. Добавьте утилиту в shell (скрипт установки покажет точные команды):

   ```bash
   # В ~/.zshrc или ~/.bashrc:
   export PATH="/путь/к/macos-sandbox:$PATH"  # Рекомендуемый способ

   # Опционально: изменить базовую директорию (по умолчанию ~/Sandbox)
   export SANDBOX_BASE_DIR="/желаемый/путь/к/песочнице"
   ```

4. Проверьте установку:
   ```bash
   sandbox --help  # Должно показать информацию об использовании
   ```

## Использование

### Workflow разработки

1. Перейдите в директорию вашего проекта внутри песочницы:

   ```bash
   cd $SANDBOX_BASE_DIR/ваш-проект
   ```

2. Используйте стандартные команды разработки через sandbox:

   ```bash
   # Node.js разработка
   sandbox npm install
   sandbox npm start
   sandbox npm run build
   sandbox npm test

   # Прямое выполнение Node.js
   sandbox node server.js
   sandbox npx create-react-app my-app

   # Git операции
   sandbox git commit -m "сообщение"
   sandbox git push

   # Любые другие инструменты разработки
   sandbox yarn dev
   sandbox python app.py
   ```

### Docker режим (опционально)

Для запуска отдельных репозиториев в Docker с изоляцией:

```bash
# Базовое использование
sandbox-docker git@github.com:user/repo.git

# Кастомные порты
sandbox-docker https://github.com/user/repo.git --ports 3000,3001,8080

# Кастомные хосты
sandbox-docker https://github.com/user/repo.git --hosts localhost,custom.host
```

## Функции безопасности

### Проверенные блокировки

**Критические системные файлы (100% заблокированы):**

- `/etc/passwd`, `/etc/shadow` - учетные данные пользователей
- `/etc/sudoers` - конфигурация sudo
- `$HOME/.ssh/` - SSH ключи
- `$HOME/.aws/` - AWS креды
- `$HOME/Documents`, `$HOME/Desktop` - личные файлы

**Доступ для разработки (разрешен):**

- `$HOME/.nvm/` - Node Version Manager
- `$HOME/.npm`, `$HOME/.yarn` - кэши пакетных менеджеров
- `$SANDBOX_BASE_DIR` - ваша рабочая область sandbox
- `/usr/local`, `/usr/bin` - системные инструменты разработки
- `/private/tmp` - временные файлы

### Как работает безопасность

Система использует динамический профиль безопасности (`security.sh`) который:

1. **Блокирует весь критический системный доступ** по умолчанию
2. **Разрешает конкретные инструменты разработки** (npm, Node.js, git)
3. **Ограничивает файловые операции** только директорией sandbox
4. **Разрешает сетевой доступ** для загрузки пакетов
5. **Предотвращает повышение привилегий** и модификации системы

### Архитектура профиля безопасности

```
sandbox.profile (генерируется динамически)
├── Системные разрешения (process*, network*)
├── Доступ к инструментам разработки (/usr/local, .nvm)
├── Рабочая область sandbox (полный доступ)
├── Кэши пакетных менеджеров (.npm, .yarn)
└── Блокировки критической системы (/etc/passwd, .ssh)
```

## Требования

- **macOS 10.15+** (тестировалось на Sonoma 14.0+)
- **Node.js через nvm** (рекомендуется для разработки)
- **Bash или Zsh**
- **Docker** (опционально, для режима контейнеров)

## Структура проекта

```
macos-sandbox/
├── sandbox.sh              # Основной скрипт выполнения
├── security.sh             # Генератор профиля безопасности
├── sandbox-setup.sh        # Скрипт установки
├── config.sh               # Управление конфигурацией
├── sandbox-docker.sh       # Docker режим (опционально)
├── sandbox-uninstall.sh    # Скрипт удаления
├── tests/                  # Набор тестов
│   ├── malware-emulator.js # Проверка безопасности
│   ├── config.bats        # Тесты конфигурации
│   ├── sandbox-docker.bats # Тесты Docker
│   └── run_tests.sh       # Запуск тестов
└── README.md              # Эта документация
```

## Разработка и тестирование

### Запуск тестов

```bash
cd tests
./run_tests.sh          # Запуск всех тестов (19 интеграционных тестов)
```

### Тестирование безопасности

Тест `malware-emulator.js` проверяет:

- **25 критических файлов** правильно заблокированы
- **18 системных файлов** имеют доступ только для чтения
- **npm workflow** функционирует корректно
- **обходы безопасности** невозможны

## Устранение неполадок

### Распространенные проблемы

1. **Ошибка "Profile not found"**

   ```bash
   ./sandbox-setup.sh  # Перегенерировать профиль
   ```

2. **Ошибка "Command must be executed inside Sandbox"**

   ```bash
   cd $SANDBOX_BASE_DIR  # Сначала перейти в sandbox
   ```

3. **npm/Node.js не работает**

   ```bash
   # Проверить доступ к nvm
   sandbox which node
   sandbox which npm

   # Перегенерировать профиль с поддержкой nvm
   ./sandbox-setup.sh
   ```

4. **Проблемы с разрешениями**
   ```bash
   # Проверить разрешения директории
   ls -la $SANDBOX_BASE_DIR
   chmod 700 $SANDBOX_BASE_DIR  # Исправить при необходимости
   ```

### Известные ограничения

- **Только macOS** - использует специфичный для платформы `sandbox-exec`
- **Некоторые системные файлы** могут быть заблокированы macOS независимо от профиля
- **Должен запускаться в** `$SANDBOX_BASE_DIR` или подкаталогах
- **Требуется nvm** для разработки Node.js (рекомендуемая настройка)

## Принцип работы

### Механизм sandbox-exec

`sandbox-exec` - это встроенный механизм безопасности macOS, основанный на технологии [Seatbelt](https://www.chromium.org/developers/design-documents/sandbox/osx-sandboxing-design/), который предоставляет:

- **Изоляцию процессов** с контролируемым системным доступом
- **Ограничения файловой системы** предотвращающие несанкционированный доступ
- **Сетевые элементы управления** для доступа к серверу разработки
- **Ограничения системных вызовов** блокирующие опасные операции

### Динамический профиль безопасности

Модуль `security.sh` генерирует профили с:

1. **Статические базовые правила** - основные системные разрешения
2. **Доступ к инструментам разработки** - npm, Node.js, git инструменты
3. **Пользовательские пути** - настроенный доступ на чтение/запись
4. **Блокировки критической системы** - файлы, чувствительные к безопасности

### Интеграция Workflow

1. **Генерация профиля**: `security.sh` создает пользовательский `sandbox.profile`
2. **Выполнение команд**: `sandbox.sh` проверяет окружение и запускает команды
3. **Обеспечение безопасности**: `sandbox-exec` применяет ограничения в реальном времени
4. **Инструменты разработки**: Полный npm/Node.js workflow с изоляцией безопасности

## Лицензия

MIT

## Возможности

- 🔒 **Полная изоляция файловой системы**
- 🌐 **Дружественный к разработке сетевой доступ**
- 📁 **Защита личных данных**
- ⚡️ **Нативная производительность** (без оверхеда Docker)
- 🛠 **Бесшовная интеграция npm/Node.js**
- 🧪 **Комплексное тестирование безопасности**
- 📋 **Стабильность готовая к продакшену**
